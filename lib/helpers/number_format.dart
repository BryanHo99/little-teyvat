/// Converts a decimal to percentage.
double decimalToPercentage(num decimal) {
  return decimal * 100;
}

/// Converts a percentage to decimal.
double percentageToDecimal(num percentage) {
  return percentage / 100;
}
